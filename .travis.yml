---
dist: bionic
language: python
# configure python version (see https://docs.travis-ci.com/user/languages/python/#specifying-python-versions)
python:
  - "3.7"

env:
  - VAGRANT_DEFAULT_PROVIDER=libvirt

services:
  - docker

# Cache the big Vagrant boxes
cache:
  directories:
    - /home/travis/.vagrant.d/boxes
    - /home/travis/.cache/pipenv

install:
  ### Vagrant installation
  # Install libvrt & KVM (see https://github.com/alvistack/ansible-role-virtualbox/blob/master/.travis.yml)
  - sudo apt-get update && sudo apt-get install -y bridge-utils dnsmasq-base ebtables libvirt-bin libvirt-dev qemu-kvm qemu-utils ruby-dev

  # Download Vagrant & Install Vagrant package
  - sudo wget -nv https://releases.hashicorp.com/vagrant/2.2.7/vagrant_2.2.7_x86_64.deb
  - sudo dpkg -i vagrant_2.2.7_x86_64.deb
  - sudo vagrant --version

  # Install vagrant-libvirt Vagrant plugin
  - sudo vagrant plugin install vagrant-libvirt
  - sudo vagrant plugin list

  # Install required (and locked) dependecies with pipenv
  - sudo -H pip install pipenv
  - sudo -H pipenv install

script:
  - cd docker

  # check, if VirtualBox config also runs with VAGRANT_DEFAULT_PROVIDER=libvirt
  - sudo -E pipenv run molecule --debug create --scenario-name vagrant-ubuntu

  # Molecule Testing Travis-locally with Docker
  - sudo -E pipenv run molecule test

  # Molecule Testing Travis-locally with Vagrant
  - sudo -E pipenv run molecule create --scenario-name vagrant-libvirt-ubuntu
  - sudo -E pipenv run molecule converge --scenario-name vagrant-libvirt-ubuntu
  - sudo -E pipenv run molecule verify --scenario-name vagrant-libvirt-ubuntu
  - sudo -E pipenv run molecule destroy --scenario-name vagrant-libvirt-ubuntu